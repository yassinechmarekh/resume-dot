# 🧠 AI Resume Builder

An intelligent resume generator powered by **AI**, built with the **MERN stack (MongoDB, Express, React (Next js), Node.js)**.  
This project allows users to create, customize, and download professional resumes with the help of an AI writing assistant and a modern, responsive UI.

---

## 🚀 Getting Started

### 1. Clone the Repository

```bash
git clone https://github.com/yassinechmarekh/ai-resume-builder.git
cd ai-resume-builder
```

### 2. Run the Client (Frontend)

```bash
cd client
```

➤ Set up environment variables

Create a .env file in the client folder and copy the variables from .env.example.

➤ Install dependencies and start the development server

```bash
npm install
npm run dev
```

The frontend will be available at:
👉 http://localhost:3000 (Default port on Next JS)

### 3. Run the Server (Backend)

Open a new terminal in the project root, then run:

```bash
cd server
```

➤ Set up environment variables

Create a .env file in the server folder and copy the variables from .env.example.

➤ Install dependencies and start the backend server

```bash
npm install
npm run dev
```

The backend will be available at:
👉 http://localhost:8000 (or the port defined in your .env file)